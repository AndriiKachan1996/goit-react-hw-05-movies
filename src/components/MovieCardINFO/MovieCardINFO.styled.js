import styled from '@emotion/styled';

export const InfoLineWrapp = styled.p`
  display: flex;
  gap: 25px;
  font-size: 30px;
  font-weight: 500;
  font-family: math;
  margin: auto;
  &:not(:last-child) {
    margin-bottom: 70px;
  }
`;

export const Title = styled.span`
  font-weight: 700;
`;
